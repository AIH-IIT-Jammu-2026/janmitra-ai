from backend.app.core.logging import logger

def retrieve_context(query: str, top_k: int = 3) -> list[str]:
    """
    RAG retriever function placeholder
    """
    logger.info(f"Retrieving context for query: {query}")
    return []
